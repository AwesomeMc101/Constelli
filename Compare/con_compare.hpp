#pragma once
#include "include.h"

namespace Compare
{
	std::string comp_strings(std::string str1, std::string str2, INST comptype);
	std::string comp_integers(std::string str1, std::string str2, INST comptype);
}
